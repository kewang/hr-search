extends layout

block content
  .container
    .pull-right
      form.form-inline(action="/upload", method="post", enctype="multipart/form-data")
        .form-group
          input.btn(name="email", type="file")
        .form-group
          input.btn.btn-primary(type="submit", value="Upload")
    table.table
      tr
        th ID
        th Name
        th Email
        th Favorite Counts
      each employee in employees
        tr
          td= employee.id
          td= employee.name
          td= employee.email
          td 71